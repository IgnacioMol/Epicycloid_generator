export interface PatternParams {
  R: number;          // Radio externo
  r: number;          // Radio interno
  d: number;          // Distancia del punto
  speed: number;      // Velocidad angular
  color: string;
  strokeWeight: number;
  phase: number;
}